# ==========================================
# NeuroEdge Kernel API - Environment
# ==========================================

# REQUIRED: API key used by protected routes (/kernel/*)
# Send as header: X-API-Key: <value>
NEUROEDGE_API_KEY=change-this-to-a-long-random-secret-key

# OPTIONAL: per-IP rate limit (requests per minute) for protected routes
# Default in code is 60 if not set
NEUROEDGE_RATE_LIMIT_PER_MIN=120

# OPTIONAL: API listen port (for future config wiring; current cmd/api uses :8080 directly)
PORT=8080

# OPTIONAL: Go runtime tuning
GOMAXPROCS=4
GOGC=100
