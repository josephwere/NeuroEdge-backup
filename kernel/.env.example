# .env.example
# Copy this file to `.env` and fill real values.
# Do NOT commit `.env` with secrets.

NEUROEDGE_API_KEY=replace-with-32+-char-random-secret
NEUROEDGE_RATE_LIMIT_PER_MIN=120
PORT=8080
GOMAXPROCS=4
GOGC=100
