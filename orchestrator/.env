# Orchestrator server
PORT=7070
LOG_LEVEL=info

# Upstream services
KERNEL_URL=http://localhost:8080
ML_URL=http://localhost:8090

# Must match kernel NEUROEDGE_API_KEY
KERNEL_API_KEY=replace-with-a-long-random-secret
# Optional fallback (your code also reads this)
NEUROEDGE_API_KEY=replace-with-a-long-random-secret
